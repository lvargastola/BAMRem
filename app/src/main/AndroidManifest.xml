<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bamrem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".IncorrectoG2Activity"></activity>
        <activity android:name=".CorrectoG2Activity" />
        <activity android:name=".Incorrecto_VidActivity" />
        <activity android:name=".Correcto_VidActivity" />
        <activity android:name=".Game_2Activity" />
        <activity android:name=".IncorrectoActivity" />
        <activity android:name=".CorrectoActivity" />
        <activity android:name=".VideoRepActivity" />
        <activity android:name=".VideoActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".SeleccionActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>